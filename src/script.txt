BEGIN

        int contador;
        string mensagem;
        float taxa;
        boolean ativo;

        contador = 0;
        contador = 0;
        taxa = 0;
        ativo = true;
        if (contador == 0) {
            mensagem = "Contador inicializado";
            print(mensagem);
        } else {
            mensagem = "Contador já possui valor";
            print(mensagem);
        }

        while (contador < 10) {
            print("Contagem: " + contador);
            contador = contador + 1;
        }

        int i;
        for (i = 0; i < 5; i = i + 1) {
            print("Iteração do loop for: " + i);
        }

        scan(int cont);
        scan(string mens);
        scan(float tax);

        if (ativo == true) {
            print("O sistema está ativo.");
        } else {
            print("O sistema está inativo.");
        }

END
